<div class="nested-fields">
  <div class="field">
    <%= f.number_field :item_counts %>
  </div>
  <div class="field">
    <%= f.collection_select :item_id, Item.all, :id, :product_name %>
  </div>
  <%= link_to_remove_association "削除", f %>
</div>
